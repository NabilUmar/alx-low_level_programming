#include "main.h"
/**
 * main - Entry point;
 * Description:Function that adds two integers and returns the result
 * Return: Result
 */
int add(int a, int b)
{
int res;

res = a + b;
return (res);
}
